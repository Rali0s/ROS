import React from "react";
import ReactDOM from "react-dom/client";
import ROSLandingPage from "./index.jsx";
import "../src/index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ROSLandingPage />
  </React.StrictMode>,
);
